mutation DownloadAllPlaylists {
  downloadAllPlaylists {
    success
    message
  }
}

mutation DeletePlaylist($playlistId: Int!) {
  deletePlaylist(playlistId: $playlistId) {
    success
    message
  }
}

query GetPlaylistInfo($url: String!) {
  playlistInfo(url: $url) {
    name
    ownerName
    trackCount
    imageUrl
    provider
  }
}
