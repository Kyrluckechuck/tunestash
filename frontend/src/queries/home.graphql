query GetSystemStatus {
  systemHealth {
    canDownload
    downloadBlockerReason
    authentication {
      cookiesValid
      cookiesErrorType
      cookiesExpireInDays
      poTokenConfigured
      spotifyUserAuthEnabled
      spotifyAuthMode
    }
    spotifyRateLimit {
      isRateLimited
      rateLimitedUntil
      secondsUntilClear
      windowCallCount
      windowMaxCalls
      windowUsagePercent
    }
    storage {
      path
      exists
      isWritable
      availableGb
      usagePercent
      isLow
      isCriticallyLow
      errorMessage
    }
  }
  queueStatus {
    totalPendingTasks
    queueSize
  }
}

mutation DisconnectSpotify {
  disconnectSpotify {
    success
    message
  }
}
