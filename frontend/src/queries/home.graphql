query GetSystemStatus {
  systemHealth {
    canDownload
    downloadBlockerReason
    authentication {
      cookiesValid
      cookiesErrorType
      cookiesExpireInDays
      poTokenConfigured
    }
  }
  queueStatus {
    totalPendingTasks
    queueSize
  }
}
