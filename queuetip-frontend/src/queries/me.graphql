query Me {
  me {
    id
    displayName
    createdAt
    isAdmin
    externalServices {
      service
      serviceUserId
      linkedAt
    }
  }
}

mutation InviteToQueuetip($email: String!) {
  inviteToQueuetip(email: $email) {
    sent
    message
  }
}
