query PlaylistByInviteToken($token: String!) {
  playlist(inviteToken: $token) {
    id
    name
    description
    members {
      account {
        id
        displayName
      }
      role
    }
  }
}

mutation JoinPlaylist($token: String!) {
  joinPlaylist(inviteToken: $token) {
    id
  }
}
