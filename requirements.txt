# Django and related packages
django==5.2.6
django-stubs[compatible-mypy]==5.2.5
django-stubs-ext==5.2.5
# Celery with database broker (PostgreSQL)
celery>=5.3.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0
kombu[sqlalchemy]>=5.3.0
sqlalchemy>=2.0.0
bx_django_utils

# Database
psycopg2-binary>=2.9.0

# Utilities
pybase62>=1.0.0

# Runtime type checking and validation
pydantic>=2.0.0


pydantic-settings>=2.0.0

# GraphQL API (updated for Python 3.13 compatibility)
strawberry-graphql==0.282.0
strawberry-graphql-django>=0.65.0

# YouTube Music API for premium detection
ytmusicapi>=1.8.0

# FastAPI and related (compatible with spotdl 4.4.2 constraints)
fastapi>=0.103.0,<0.104.0
uvicorn>=0.23.2,<0.24.0
uvicorn[standard]>=0.23.2,<0.24.0  # Includes httptools and websockets
python-dotenv==1.1.1
rich>=13.9.4,<14.0.0
typer==0.19.2
click==8.3.0
requests==2.32.5

# Authentication
PyJWT==2.10.1
passlib[bcrypt]==1.7.4

# Test dependencies
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-django==4.11.1
pytest-xdist==3.8.0
coverage==7.10.7
factory-boy>=3.3.0
faker>=22.0.0

# Essential dependencies from root requirements.txt
click
dynaconf
pymediainfo
pyyaml
# The following are useful in development but not required at runtime.
# They live in requirements-dev.txt to avoid inflating the runtime image.
# django-debug-toolbar
# django-extensions
# pylint-django

# Critical: spotdl and yt-dlp (using current versions)
# Note: FastAPI/Starlette/Rich constraints from spotdl are bypassed above
spotdl==4.4.2
yt-dlp==2025.9.23

# Development and linting tools
flake8>=6.0.0
black==25.9.0
isort>=5.12.0
pylint==3.3.8
bandit>=1.7.0
mypy==1.18.2
whitenoise==6.11.0
types-passlib
types-pyasn1 
