# =============================================================================
# Production Dependencies Only
# =============================================================================
# These are the minimal dependencies required to run the application in production.
# Dev/test dependencies are in requirements-dev.txt

# Django and related packages
django==5.2.8
django-extensions>=3.2.0  # Provides shell_plus for easier debugging
# Celery with database broker (PostgreSQL)
# 5.6.2 fixes DjangoWorkerFixup AttributeError (PR #10045)
celery>=5.6.2
django-celery-beat>=2.5.0
django-celery-results>=2.5.0
kombu[sqlalchemy]>=5.6.0
sqlalchemy>=2.0.0

# Database
psycopg2-binary>=2.9.0

# Utilities
pybase62>=1.0.0
psutil>=5.9.0  # Process and system monitoring for worker diagnostics

# GraphQL API (updated for Python 3.13 compatibility)
strawberry-graphql==0.287.0
strawberry-graphql-django>=0.67.0

# FastAPI and related (compatible with spotdl 4.4.2 constraints)
fastapi>=0.103.0,<0.104.0
uvicorn>=0.23.2,<0.24.0
uvicorn[standard]>=0.23.2,<0.24.0  # Includes httptools and websockets

# Direct dependencies (also transitive from spotdl)
pydantic>=2.0.0
requests>=2.28.0

# Essential dependencies
dynaconf
pymediainfo
pyyaml

# Critical: spotdl and yt-dlp (using current versions)
# spotdl brings: pydantic, rich, requests, ytmusicapi, click (via typer)
spotdl==4.4.3
yt-dlp[default]>=2025.11.12  # Includes yt-dlp-ejs for JavaScript execution (YouTube bot protection)
